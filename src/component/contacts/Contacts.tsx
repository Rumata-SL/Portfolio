import React from "react";
import c from "./Contacts.module.css"

export const Contacts = () => {
    return (
        <div className={c.wrapper}>
            <div className={c.container}>Contacts</div>
        </div>
    );
}

