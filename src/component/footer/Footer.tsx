import React from "react";
import f from "./Footer.module.css"

export const Footer = () => {
    return (
        <div className={f.wrapper}>
         <footer className={f.container}>Footer</footer>
        </div>
    );
};
